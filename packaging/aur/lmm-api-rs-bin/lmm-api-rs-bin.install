#!/usr/bin/env bash
# Package installation is deliberately host-neutral.  The fallback target is
# authorized only when an operator invokes a packaged deployment entry point;
# those scripts run fallback-target-guard.sh before touching state or services.

post_install() {
  :
}

post_upgrade() {
  :
}
