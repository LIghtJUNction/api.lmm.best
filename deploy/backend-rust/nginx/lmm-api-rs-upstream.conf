# Managed atomically by deploy-lmm-api-rs.sh. This initial value is intentionally
# invalid as a production target; installation must replace it with a validated slot.
upstream lmm_api_rs_active {
    server 127.0.0.1:9;
    keepalive 32;
}
