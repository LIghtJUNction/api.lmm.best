# Non-secret identity for the isolated fallback.lmm.best test instance.
# DATABASE_URL, VALKEY_URL, SESSION_SECRET, and cookie origins belong in the
# separate 0600 common.env copied from test-instance.env.example.
LMM_RS_LISTEN_ADDR=127.0.0.1:3100
LMM_RS_SLOT=blue
