# Include once in nginx's `http` context (not inside a server block).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Optional host-level policies are deliberately separate from application
# routing and may be installed by the server administrator.
include /etc/nginx/site-policy/http/*.conf;
